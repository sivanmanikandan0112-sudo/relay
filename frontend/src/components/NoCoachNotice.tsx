export function NoCoachNotice() {
  return (
    <section>
      <p className="eyebrow-mono">NOT YET CONNECTED</p>
      <h1 className="page-title">Waiting on a coach.</h1>
      <p className="page-subtitle">
        Your account isn't on a coach's roster yet, so check-ins and run logging aren't available.
        Once a coach adds you, you'll get access to daily check-ins, your run log, and how it all
        works.
      </p>
    </section>
  );
}
